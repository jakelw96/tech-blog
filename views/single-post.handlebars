{{> post-data post}}

{{#if loggedIn }}
<div class="container">
    <form class="comment-box">
        <div>
            <textarea name="comment-data"></textarea>
        </div>
        <button type="submit" class="btn">Add A Comment</button>
    </form>
</div>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn }}
<script src="/javascript/comment.js"></script>
{{/if}}